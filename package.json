{
  "name": "midi-bridge",
  "main": "src/main.ts",
  "type": "module",
  "private": true,
  "scripts": {
    "dev": "bun --watch src/main.ts",
    "build": "bun build src/main.ts --compile --bundle --minify --outfile dist/MIDI-Bridge.exe"
  },
  "devDependencies": {
    "@types/bun": "latest"
  },
  "peerDependencies": {
    "typescript": "^5"
  },
  "dependencies": {
    "jzz": "^1.9.6",
    "tonal": "^6.4.3"
  }
}
